---
---

<section id="inicio" class="py-16 md:py-20 lg:py-24 bg-gradient-to-br from-[var(--bg-secondary)] to-[var(--primary)] relative overflow-hidden transition-[background] duration-300 ease-in-out light:bg-gradient-to-br light:from-[var(--bg-primary)] light:to-[#f0f4ff] before:content-[''] before:absolute before:-top-1/2 before:-right-1/5 before:w-[1000px] before:h-[1000px] before:bg-[radial-gradient(circle,rgba(26,109,204,0.05)_0%,transparent_70%)] before:rounded-full">
  <div class="w-full max-w-[1600px] mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-5 lg:gap-6 items-center">
      <!-- Columna de texto con padding lateral -->
      <div class="relative z-[2] px-4 sm:px-6 md:px-8 lg:px-10">
        <div class="inline-flex items-center gap-2 bg-[rgba(26,109,204,0.1)] text-[var(--secondary)] px-5 py-2.5 rounded-full text-base font-semibold mb-4 border border-[rgba(26,109,204,0.3)]">✨ La solución más completa</div>

        <h1 class="text-4xl md:text-5xl lg:text-6xl font-extrabold leading-tight mb-4 text-white light:text-[var(--app-secondary)]">
          Facturación electrónica fácil, rápida y confiable
        </h1>

        <p class="text-base md:text-lg lg:text-xl leading-relaxed text-[var(--app-text-secondary)] mb-6 max-w-2xl">
          Sistema completo de facturación CFDI 4.0 con conciliación bancaria,
          envío por WhatsApp y todas las herramientas que necesitas para tu
          negocio.
        </p>

        <div class="flex gap-4 flex-wrap">
          <a
            href="https://app.solucionfactura.mx/register"
            class="inline-flex items-center gap-2 px-8 py-4 rounded-xl font-bold no-underline transition-all duration-200 ease-in-out border-none cursor-pointer text-base md:text-lg bg-gradient-to-br from-[var(--highlight-blue)] to-[var(--secondary)] text-white shadow-[var(--theme-shadow-lg)] hover:-translate-y-1 hover:shadow-[var(--theme-shadow-xl)] focus:outline-2 focus:outline-[var(--secondary)] focus:outline-offset-2"
          >
            🚀 Prueba Gratis
          </a>
          <a href="#caracteristicas" class="inline-flex items-center gap-2 px-8 py-4 rounded-xl font-bold no-underline transition-all duration-200 ease-in-out cursor-pointer text-base md:text-lg bg-transparent text-white border-2 border-white hover:bg-white/10 hover:-translate-y-1 hover:shadow-[var(--theme-shadow-lg)] focus:outline-2 focus:outline-[var(--secondary)] focus:outline-offset-2 light:text-[var(--text-primary)] light:border-[var(--text-primary)] light:hover:bg-[var(--text-primary)]/10">
            Ver Características
          </a>
        </div>
      </div>

      <!-- Columna de video que se extiende hasta el borde -->
      <div class="relative flex justify-center items-center mt-6 md:mt-0 md:pr-0">
        <div data-video-container class="relative w-full aspect-video md:rounded-none overflow-hidden cursor-pointer shadow-[var(--theme-shadow-xl)]">
          <!-- Thumbnail que se muestra inicialmente -->
          <picture>
            <source srcset="/thumbnail.webp" type="image/webp" />
            <img
              src="/thumbnail.webp"
              alt="Dashboard del sistema Solución Factura - Vista de reportes y estadísticas"
              data-video-thumbnail
              class="w-full h-full object-cover md:rounded-none transition-opacity duration-500"
              width="650"
              height="365"
              loading="eager"
            />
          </picture>

          <!-- Video que se carga después -->
          <video
            src="/sf-video.mp4"
            data-hero-video
            class="w-full h-full object-cover md:rounded-none transition-opacity duration-500"
            width="650"
            height="365"
            muted
            loop
            playsinline
            preload="none"
            style="display: none; opacity: 0;"
          >
            Tu navegador no soporta el video.
          </video>

          <!-- Botón de play opcional -->
          <div data-play-button class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 bg-black/70 rounded-full w-16 h-16 flex items-center justify-center transition-all duration-300 backdrop-blur-[10px] hover:bg-black/80 hover:scale-110" aria-label="Reproducir video">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="white">
              <path d="M8 5v14l11-7z"></path>
            </svg>
          </div>
        </div>

        <style>
          [data-video-container][data-playing] [data-play-button] {
            opacity: 0;
            pointer-events: none;
          }
        </style>

        <script>
          document.addEventListener("DOMContentLoaded", function () {
            const container = document.querySelector("[data-video-container]");
            const thumbnail = document.querySelector("[data-video-thumbnail]");
            const video = document.querySelector("[data-hero-video]");
            const playButton = document.querySelector("[data-play-button]");

            // Auto-cargar video cuando sea visible
            const observer = new IntersectionObserver(
              (entries) => {
                entries.forEach((entry) => {
                  if (entry.isIntersecting) {
                    // Pre-cargar video en background
                    video.preload = "auto";
                    video.load();
                    observer.unobserve(container);
                  }
                });
              },
              {
                threshold: 0.1,
                rootMargin: "100px",
              },
            );

            observer.observe(container);

            // Función para iniciar video
            function startVideo() {
              container.setAttribute("data-playing", "");
              video.style.display = "block";

              video
                .play()
                .then(() => {
                  // Transición suave de thumbnail a video
                  setTimeout(() => {
                    video.style.opacity = "1";
                    thumbnail.style.opacity = "0";
                  }, 100);

                  // Ocultar thumbnail después de la transición
                  setTimeout(() => {
                    thumbnail.style.display = "none";
                  }, 600);
                })
                .catch(() => {
                  // Si falla el autoplay, mostrar solo el botón
                  console.log("Autoplay bloqueado");
                });
            }

            // Click en cualquier parte del contenedor inicia el video
            container.addEventListener("click", startVideo);

            // También auto-iniciar después de que el video esté cargado
            video.addEventListener("canplaythrough", () => {
              // Auto-iniciar solo si está visible
              const rect = container.getBoundingClientRect();
              const isVisible =
                rect.top < window.innerHeight && rect.bottom > 0;

              if (isVisible) {
                setTimeout(startVideo, 1000); // Esperar 1 segundo después de cargar
              }
            });
          });
        </script>
      </div>
    </div>
  </div>
</section>
